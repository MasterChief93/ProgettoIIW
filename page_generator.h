#ifndef PAGE_GENERATOR_H                         //Include Guards: to avoid multiple header inclusions (having so functions defined multiple times) -Include Guards per evitare di includere più volte l'header (ed avere quindi funzioni definite più volte)7
#define PAGE_GENERATOR_H

extern int page_generator(struct Config *cfg);

#endif